## Redis notes
- In prod, we will need a password. That has to be enabled in docker compose using a command. 
- Volumes are optional. In our case, since we are going to implement rate limiting, we might need. 